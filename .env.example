# 프론트 환경변수 예시 (실제 값은 .env 에 두고 커밋하지 않음)
# 목(mock) 모드: 미설정 또는 true → 로컬 JSON 사용 / false → 실서버 호출
VITE_USE_MOCK=false
# 백엔드 Base URL (로컬: http://localhost:8000, 배포: Render URL)
VITE_API_BASE_URL=http://localhost:8000
